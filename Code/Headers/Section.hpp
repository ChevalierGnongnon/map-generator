#pragma once
#include "Header.hpp"

class Section{
    private:
        std::vector<t_connector>    connectors;
        size_t                      width;
        size_t                      height;
};